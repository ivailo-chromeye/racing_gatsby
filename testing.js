const axios = require("axios");
const client = axios.create({
  baseURL: "https://s3.eu-west-2.amazonaws.com/racipngpost.json.data.lambda/",
});

const { getFlatRaces } = require("./raceFunctions.js");






;(async() => {
  // flat array
  


  // 
  // results.map(result => {
  //   console.log(result.data);
  // })



})();
