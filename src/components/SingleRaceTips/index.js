import React, { useState } from "react";
import Layout from "../layout";

const SingleRaceTips = ({ pageContext, location }) => {
  
  return (
    <Layout>
      single race tips
    </Layout>
  )
}

export default SingleRaceTips;