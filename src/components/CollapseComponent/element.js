import React from 'react';

const CollapseEl = ({ children }) => {
  return (
    <div style={{margin: "16px 0px"}}>
      <span>
        {children}
      </span>
    </div>
  )
}

export default CollapseEl
